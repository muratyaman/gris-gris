{
  "name": "example-http2-node-client",
  "version": "1.0.0",
  "description": "example http2 client on node to work with gris-gris",
  "main": "build/index.js",
  "scripts": {
    "build:ts": "tsc -p ./tsconfig.json",
    "start:dev": "ts-node ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
